
let userName: string[] = ['admin', 'Eric', 'Ali', 'Aysha','Asma'];

if(userName.length === 0){

    console.log('we need to find some users');
}
else{
userName = []; 
    console.log('All user name has been removed');
}